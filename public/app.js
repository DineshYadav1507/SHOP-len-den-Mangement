let customers = [];
let selected = null;
let lang = localStorage.getItem('shop-lang') || 'hi';
const $ = s => document.querySelector(s);
const money = n => `₹${Number(n || 0).toLocaleString('en-IN')}`;
const esc = s => String(s ?? '').replace(/[&<>\"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
async function api(url, opts={}) { const r=await fetch(url,{headers:{'Content-Type':'application/json',...(opts.headers||{})},...opts}); const d=await r.json().catch(()=>({})); if(!r.ok) throw new Error(d.error||'Something went wrong'); return d; }
function toast(msg){ const el=$('#toast'); el.textContent=msg; el.classList.add('show'); setTimeout(()=>el.classList.remove('show'),3000); }
function render(){
  const q=$('#search').value.toLowerCase();
  const list=customers.filter(c=>`${c.name} ${c.phone}`.toLowerCase().includes(q));
  $('#customers').innerHTML=list.length?list.map(c=>`<article class="customer" data-id="${c.id}">
    <div class="avatar">${esc(c.name[0]||'?').toUpperCase()}</div><div class="c-main"><div class="c-name">${esc(c.name)}</div><div class="phone">${esc(c.phone)}</div><div class="balance ${c.balance<0?'advance':''}">${c.balance>=0?`Lena hai / Due ${money(c.balance)}`:`Dena hai / Pay ${money(Math.abs(c.balance))}`}</div></div><div class="card-actions"><button class="small" data-action="open">Khata</button><button class="wa-mini" data-action="remind">WA</button></div>
  </article>`).join(''):`<div class="empty"><b>No customers yet</b><span>Add your first customer to start the khata.</span></div>`;
  $('#sCustomers').textContent=customers.length; $('#sGiven').textContent=money(customers.reduce((a,c)=>a+c.balance,0)); $('#sReceived').textContent=money(0);
}
async function load(){ const d=await api('/api/dashboard'); customers=d.customers; $('#sGiven').textContent=money(d.totals.given); $('#sReceived').textContent=money(d.totals.received); render(); updateWa(d.whatsapp); }
function updateWa(w){ $('#waBtn').textContent=w.connected?`✓ WhatsApp ${w.phone||'Connected'}`:'WhatsApp Connect'; $('#waBtn').classList.toggle('connected',w.connected); }
function openModal(html){ $('#modalBody').innerHTML=html; $('#modal').classList.remove('hidden'); }
function closeModal(){ $('#modal').classList.add('hidden'); selected=null; }
function addCustomer(){ openModal(`<h2>Add Customer</h2><p class="muted">Customer / party details</p><form id="customerForm"><label>Name<input name="name" required placeholder="Rahul Sharma" /></label><label>Mobile<input name="phone" required inputmode="numeric" placeholder="9876543210" /></label><label>Language<select name="language"><option value="hi">Hindi / Hinglish</option><option value="en">English</option></select></label><button class="primary full">Save Customer</button></form>`); $('#customerForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{await api('/api/customers',{method:'POST',body:JSON.stringify(Object.fromEntries(f))});closeModal();await load();toast('Customer added');}catch(err){toast(err.message)}}; }
async function openCustomer(id){ const d=await api(`/api/customers/${id}`); selected=d; openModal(`<div class="khata-head"><div><h2>${esc(d.customer.name)}</h2><div class="muted">${esc(d.customer.phone)}</div></div><button class="outline" id="remind">WhatsApp Reminder</button></div><div class="big-balance ${d.customer.balance<0?'advance':''}">${money(Math.abs(d.customer.balance))}<small>${d.customer.balance>=0?'Lena hai / Due':'Dena hai / Advance'}</small></div><div class="ledger-actions"><button class="debit" id="debit">+ Debit / Lena</button><button class="credit" id="credit">− Credit / Jama</button></div><div class="tx-list">${d.transactions.length?d.transactions.map(t=>`<div class="tx"><div><b>${t.type==='DEBIT'?'Debit / Lena':'Credit / Jama'}</b><small>${new Date(t.created_at).toLocaleString('en-IN')} ${t.note?'· '+esc(t.note):''}</small></div><strong class="${t.type==='DEBIT'?'plus':'minus'}">${t.type==='DEBIT'?'+':'−'}${money(t.amount)}</strong></div>`).join(''):'<div class="empty compact">No transactions yet.</div>'}</div>`); $('#debit').onclick=()=>addTx('DEBIT'); $('#credit').onclick=()=>addTx('CREDIT'); $('#remind').onclick=async()=>{try{await api(`/api/customers/${id}/send-reminder`,{method:'POST'});toast('WhatsApp reminder sent');}catch(e){toast(e.message)}}; }
function addTx(type){ const title=type==='DEBIT'?'Debit / Lena Hai':'Credit / Jama Hua'; openModal(`<h2>${title}</h2><p class="muted">${esc(selected.customer.name)}</p><form id="txForm"><label>Amount<input name="amount" type="number" min="1" step="1" required placeholder="5000" /></label><label>Note<input name="note" placeholder="Mal ka payment / Cash" /></label><label class="check"><input name="sendWhatsApp" type="checkbox" checked /> WhatsApp par confirmation bheje</label><button class="primary full">Save & Send</button></form>`); $('#txForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{const d=await api(`/api/customers/${selected.customer.id}/transactions`,{method:'POST',body:JSON.stringify({type,amount:f.get('amount'),note:f.get('note'),sendWhatsApp:f.has('sendWhatsApp')})});closeModal();await load();toast(d.whatsapp.sent?'Len-den saved + WhatsApp sent':'Len-den saved; WhatsApp not connected');}catch(err){toast(err.message)}}; }
async function connectWa(){ await api('/api/whatsapp/connect',{method:'POST'}); const poll=setInterval(async()=>{const w=await api('/api/whatsapp/status');updateWa(w);if(w.qr){clearInterval(poll);openModal(`<h2>Connect WhatsApp</h2><p class="muted">Phone → Linked devices → Link a device → Scan this QR.</p><img class="qr" src="${w.qr}" alt="WhatsApp QR" /><p class="muted">QR is generated by the WhatsApp Web connector. Keep this page open.</p>`)}else if(w.connected){clearInterval(poll);closeModal();toast('WhatsApp connected');}},1000); setTimeout(()=>clearInterval(poll),60000); }
$('#addBtn').onclick=addCustomer; $('#search').oninput=render; $('#waBtn').onclick=connectWa; $('[data-close]').onclick=closeModal; $('#modal').onclick=e=>{if(e.target.id==='modal')closeModal()}; $('#customers').onclick=e=>{const card=e.target.closest('.customer');if(!card)return;const id=card.dataset.id;if(e.target.dataset.action==='remind'){api(`/api/customers/${id}/send-reminder`,{method:'POST'}).then(()=>toast('WhatsApp reminder sent')).catch(x=>toast(x.message));}else openCustomer(id);}; $('#langBtn').onclick=()=>{lang=lang==='hi'?'en':'hi';localStorage.setItem('shop-lang',lang);$('#langBtn').textContent=lang==='hi'?'हिंदी':'English';};
load().catch(e=>toast(e.message)); setInterval(async()=>{try{const w=await api('/api/whatsapp/status');updateWa(w)}catch{}},5000);
